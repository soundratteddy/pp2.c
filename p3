#include <stdio.h>

int main(void) {
int i,n;
unsigned long long factorial=1;
scanf("%d",&n);
  if(n<0)
  printf("erro num");
  else
  {
    for(i=1;i<=n;++i)
    {
      factorial *=i;
    }
    printf("fac of %d = %llu",n,factorial);
  }
  return 0;
}
